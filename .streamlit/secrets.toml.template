# secrets.toml.template
# ======================
# Copie este arquivo para .streamlit/secrets.toml e preencha os valores.
# NUNCA commite o secrets.toml real no git.
#
# Gerar senha: python -c "import secrets; print(secrets.token_urlsafe(32))"

[app]
# Senha de acesso ao app. Deixe vazio ("") para desabilitar autenticação
# (útil para desenvolvimento local sem secrets.toml configurado).
password = "TROQUE_POR_UMA_SENHA_FORTE"
